{
  "name": "doare-coffee-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute DB --local --file=schema.sql && wrangler d1 execute DB --local --file=seed.sql && wrangler d1 execute DB --local --file=seed-admin.sql"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
